[k3s_servers]
%{ for node in k3s_servers_inventory ~}
${node.name} ansible_host=${node.default_ipv4_address}
%{ endfor ~}

[k3s_workers]
%{ for node in k3s_workers_inventory ~}
${node.name} ansible_host=${node.default_ipv4_address}
%{ endfor ~}

